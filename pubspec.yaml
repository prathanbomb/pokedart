name: pokeapi
description: PokeAPI Package
version: 1.0.4
homepage: "https://github.com/prathanbomb/pokedart"

environment:
    sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.24.9
